// Question 31

let usernames1 = ["Abdullah", "Ali", "Ahmed", "admin", "Talha"];
usernames1.length = 0;
if (usernames1.length == 0) {
  console.log("No users found. We need to find some users");
} else {
  console.log("We have some users");
}
