// Question 41

let magicianNames = ["M1", "M2", "M3", "M4"];

function show_magicians2(names: string[]): any {
  for (let name of names) {
    console.log(name);
  }
}

show_magicians2(magicianNames);
